# OpenTelemetry GenAI 详解

> 整理自本仓库 `research/otel/` 下的上游规范快照（OpenTelemetry GenAI 语义约定全文）。

## 一句话定位

OpenTelemetry GenAI 是 OTel 语义约定（semantic conventions）中专为**生成式 AI / LLM / Agent** 定义的一套遥测 schema：它不规定你用什么 SDK，而是规定**"LLM 调用、工具调用、agent 编排、检索、记忆"这些操作应该产出什么样名字的 span、什么样的属性键、什么样的指标和事件**。目的是让不同框架（LangChain、ADK、OpenAI Agents…）产出的遥测可以互换、可被同一套看板和告警消费，而不是每家自造埋点。

## 为什么需要它

传统 OTel 约定（HTTP、DB、消息队列）无法表达几个 GenAI 特有维度：

- **成本与用量**：token 类型（输入 / 输出 / 缓存读 / 缓存写）、按模态拆分（文本 / 图像 / 音频）。
- **非确定性**：同一个 span 需要留下 model、temperature、top_p、finish_reason 等"抽样配置"，否则问题无法复现。
- **内容敏感性**：prompt / response 既是排障刚需，又是 PII 与合规风险，必须显式 opt-in。
- **Agent 特有的失败模式**：绕圈（loop）、选错工具、规划错误——这些只能靠工具调用与规划阶段的 span 暴露。
- **内容可能极大**：需要"属性内联"与"外存 + 引用"两种模式并存。

## 治理与成熟度（很关键）

- GenAI 约定已从主仓库迁出，独立为 `open-telemetry/semantic-conventions-genai`（本地 `research/otel/` 就是它的全文快照）。
- **全部条目的状态目前是 `Development`**，不是 `Stable`。这意味着后台的字段仍会改（见下文"破坏性变更"），落地时必须把埋点层隔离封装，不要把 `gen_ai.*` 直接写死在业务代码里。
- 规范由 **Weaver** 从 machine-readable registry 自动生成 Markdown（`*_attributes__gen-ai.md`、`gen-ai-metrics.md` 里的 autogenerated 注释即为此），因此有 JSON schema 与 registry 文件可做**代码生成**。
- 有独立的一致性测试基建：`reference/scenarios/<library>/` 用真实 SDK + 本地 mock server 跑出遥测，再用 Weaver 校验是否符合约定，产出 `data.json` 与各 span / metric 的覆盖报告。**这是评估"某框架埋点是否规范"的最佳依据。**

## 信号模型

| 信号 | 内容 |
| --- | --- |
| Spans | 客户端侧操作（模型 / 检索 / 记忆 / 工具）+ agent 框架侧操作（创建 / 调用 / 规划 / 工作流） |
| Metrics | client、server、workflow、agent、tool 五类 |
| Events | `gen_ai.client.inference.operation.details`、`gen_ai.evaluation.result` |
| Exceptions | `gen_ai.client.operation.exception`（建议日志级别 WARN / severity 13） |
| MCP | 独立章节，但同属该约定族（span + 4 个 duration 指标） |

## Span 模型

两类文档分工：`research/otel/docs__gen-ai__gen-ai-spans.md`（模型调用侧）与 `research/otel/docs__gen-ai__gen-ai-agent-spans.md`（agent / 框架侧）。后者**扩展并覆盖**前者。

模型侧：

| Span | `gen_ai.operation.name` | 用途 |
| --- | --- | --- |
| Inference | `chat` / `text_completion` / `generate_content` | 主要成本与延迟来源 |
| Embeddings | `embeddings` | 向量化 |
| Retrieval | `retrieval` | RAG 检索质量、top_k |
| Fetch response | `fetch_response` | 按 ID 取回已生成响应（不产生推理、**不应上报 token 用量**） |
| Memory | `create_memory` / `update_memory` / `upsert_memory` / `search_memory` / `delete_memory` / `create_memory_store` / `delete_memory_store` | 记忆生命周期 |
| Execute tool | `execute_tool` | 工具执行（两侧文档均有定义） |

Agent 侧：

| Span | `gen_ai.operation.name` | 关键区别 |
| --- | --- | --- |
| Create agent | `create_agent` | 面向远程 agent 服务；记录 `gen_ai.agent.id` |
| Invoke agent (client) | `invoke_agent` | 跨进程调用，span kind `CLIENT` |
| Invoke agent (internal) | `invoke_agent` | 框架进程内调用；**不要求** `provider.name`，**不记录** `agent.version` 与缓存 token 明细 |
| Invoke workflow | `invoke_workflow` | 工作流 / 编排（如 LangGraph、ADK） |
| Plan | `plan` | 规划与任务分解，用于区分"想错了"还是"做错了" |

## 命名与标识约定

- **Span name**：模型侧 `{gen_ai.operation.name} {gen_ai.request.model}`；agent 侧如 `create_agent {gen_ai.agent.name}`；MCP 侧为 `{mcp.method.name} {target}`（无 low-cardinality target 时退化为 `{mcp.method.name}`）。
- **Span kind**：常规为 `CLIENT`，同进程内运行的模型可标 `INTERNAL`。
- **一个 span 覆盖整个逻辑操作的耗时，包含自动重试**（这条常被实现错）。
- **采样相关属性必须在 span 创建时提供**，否则尾部采样看不到：`gen_ai.operation.name`、`gen_ai.provider.name`、`gen_ai.request.model`、`gen_ai.agent.name`、`server.address/port`。
- **`gen_ai.conversation.id`**：只有**真实存在**会话 ID 时才写（框架的 session_id、OpenAI thread、Bedrock session 等）；**禁止**用新生成的 UUID、trace id 或内容 hash 兜底——这条是硬性澄清，否则会话聚合会被污染。

## 属性命名空间速查

| 命名空间 | 内容 |
| --- | --- |
| `gen_ai.request.*` | model、temperature、top_p、top_k、max_tokens、stop_sequences、choice.count、reasoning.level、previous_response.id |
| `gen_ai.response.*` | id、model、finish_reasons、time_to_first_chunk |
| `gen_ai.usage.*` | 按模态与缓存细分 |
| `gen_ai.tool.*` | name、type、description、definitions、call.id、call.arguments、call.result |
| `gen_ai.memory.*` | record.id、records、store.id |
| `gen_ai.embeddings.dimension.count` | 嵌入维度 |
| `gen_ai.data_source.id` | 检索数据源 |
| `gen_ai.prompt.*` | name、version、variable |
| `gen_ai.evaluation.*` | name、score.value、score.label、explanation |
| `gen_ai.agent.*` | id、name、description、version |
| `gen_ai.conversation.*` | id、compacted |
| `gen_ai.provider.name` / `gen_ai.operation.name` | 供应商与操作判别符 |
| `gen_ai.output.type` / `gen_ai.token.type` | 输出类型 / token 类型 |
## 敏感内容的处理（实践中最重要的设计）

`gen_ai.system_instructions` / `gen_ai.input.messages` / `gen_ai.output.messages` **默认绝不采集**，instrumentation 只提供开关。规范给了三种模式：

1. **默认**：只记元数据，不记内容。
2. **内容内联到 span 属性**——适合预发或合规允许的存储；结构必须符合官方 JSON schema（含 `text` / `tool_call` / `document` / `CompactionPart` 等 part 类型）。
3. **外存 + 引用**（生产推荐）：内容存到可独立授权访问的存储，span 上只留引用，规避遥测后端体积与访问模型的冲突。

注意 `gen_ai.tool.description` 与 `gen_ai.tool.definitions` 同样被标注可能含敏感信息（工具 schema 会泄露内部系统结构）。

## 指标

| 指标 | 类型 | 单位 | 分桶 |
| --- | --- | --- | --- |
| `gen_ai.client.token.usage` | Histogram | `{token}` | 1 … 67108864 |
| `gen_ai.client.operation.duration` | Histogram | s | 0.01 … 81.92 |
| `gen_ai.client.operation.time_to_first_chunk` | Histogram | s | 同上 |
| `gen_ai.client.operation.time_per_output_chunk` | Histogram | s | 同上 |
| `gen_ai.server.request.duration` / `time_per_output_token` / `time_to_first_token` | — | — | 模型服务端视角 |
| `gen_ai.invoke_workflow.duration` | Histogram | s | 1 … 7200 |
| `gen_ai.invoke_agent.duration` | Histogram | s | 长桶 |
| `gen_ai.invoke_agent.inference_calls` / `.tool_calls` | 计数 | `{call}` | 单次 agent 调用内的调用次数 |
| `gen_ai.execute_tool.duration` | Histogram | s | 长桶 |

两点实务提醒：

1. token 用量在"计费 token"与"消耗 token"并存时，**必须上报计费口径**；
2. `inference_calls` / `tool_calls` 是发现"agent 绕圈"最早、最可靠的信号，非常适合做告警基线。

## 事件与异常

- **`gen_ai.client.inference.operation.details`**：把一次推理的完整细节（请求参数、用量、可选内容）作为事件记录，适合"span 上不想放重属性，但希望可查"。
- **`gen_ai.evaluation.result`**：把评测分数写进 trace 的运行期事件——这是 2026 年"评测对象从答案变为 trace"的标准载体，可挂在 `plan` / `execute_tool` / `retrieval` 上。
- **`gen_ai.client.operation.exception`**：异常事件，附带 `exception.type` / `message` / `stacktrace`。

## MCP 与厂商专属扩展

- **MCP**：client / server 两侧 span，名称 `{mcp.method.name} {target}`，指标 `mcp.client.operation.duration`、`mcp.server.operation.duration`、`mcp.client.session.duration`、`mcp.server.session.duration`，并定义了 stdio 与 Streamable HTTP 的传输记录方式。
- **厂商专属**：`providers` 文档覆盖 Anthropic、AWS Bedrock、Azure AI Inference、OpenAI。`gen_ai.provider.name` 是**判别符**：设为 `aws.bedrock` 就应该配 `aws.bedrock.*` 属性且**不应**出现 `openai.*` 属性；已收录 16 个知名取值（含 `azure.ai.openai`、`gcp.gemini`、`gcp.vertex_ai`、`deepseek`、`groq`、`moonshot_ai`、`x_ai`、`perplexity` 等）。

## 值得注意的近期破坏性变更

（说明为什么必须封装）

- `gen_ai.usage.cache_creation.input_tokens` → **`gen_ai.usage.cache_write.input_tokens`**。
- 新增按模态的用量：`gen_ai.usage.{text,image,audio}.{input,output}.tokens` 与 `.cache_read.input_tokens`。
- 缓存明细**从 internal `invoke_agent` span 上移除**（跨多次模型调用聚合会误导，应从 `gen_ai.inference.client` span 汇总）。
- `gen_ai.request.top_k` 由 `double` 改 `int`，并收窄为解码参数；检索改用 **`gen_ai.retrieval.top_k`**。
- `gen_ai.output.messages[].finish_reason` 废弃，统一用 **`gen_ai.response.finish_reasons`**（缺失时应填 `error`）。
- internal `invoke_agent` span 不再要求 `gen_ai.provider.name`，也不再记录 `gen_ai.agent.version`。
- 新增：`plan` 操作、`fetch_response` 操作、`gen_ai.prompt.version` / `gen_ai.prompt.variable`、`gen_ai.request.reasoning.level`、`gen_ai.request.previous_response.id`、`gen_ai.conversation.compacted`。

## 一处需要修正现有文档的地方

`gen_ai.conversation.compacted` 不是事件，它是**布尔属性**（`gen_ai.conversation.compacted: true`），落在推理 span 与 `gen_ai.client.inference.operation.details` 事件上，用来标记"本次用的是压缩后的会话上下文"。

在 `agent-production-research.md` 中，它被列进了事件（第 233 行），且在属性清单里漏掉了（第 235 行），建议一并调整。另需补充的是它配套的 `CompactionPart` 消息 schema——这是"上下文压缩"能被观测的前提。

## 落地建议

- 把 OTel GenAI 当成**接口标准**而非实现：在平台上封装一层 `otel-genai` SDK，业务只调你的封装，registry 升级时只改一层。
- 用 Weaver + registry 做**代码生成与校验**，把 `Development` 状态的漂移变成 CI 里的可见失败，而不是线上看板突然空了。
- 用 `reference/scenarios/` 的覆盖报告来选型和验收埋点库：LangChain、LangGraph、ADK、OpenAI Agents、CrewAI、Pydantic AI、LiteLLM 等都有参考实现，可直接抄它们的接法。
- 先落"零敏感"的部分（span 骨架 + 用量与延迟指标 + 工具与规划 span），内容采集单独走合规评审。

## 一手资料索引

| 文件 | 内容 |
| --- | --- |
| `research/otel/docs__gen-ai__README.md` | 总览与信号清单 |
| `research/otel/docs__gen-ai__gen-ai-spans.md` | 模型侧 span 全量定义 |
| `research/otel/docs__gen-ai__gen-ai-agent-spans.md` | agent / 框架侧 span 定义 |
| `research/otel/docs__gen-ai__gen-ai-metrics.md` | 五类指标与分桶 |
| `research/otel/docs__gen-ai__gen-ai-events.md` | 事件定义 |
| `research/otel/docs__gen-ai__gen-ai-exceptions.md` | 异常定义 |
| `research/otel/docs__gen-ai__mcp.md` | MCP 约定 |
| `research/otel/docs__registry__attributes__gen-ai.md` | 属性总表（最适合做代码生成输入） |
| `research/otel/reference__README.md` | 各语言 / 框架一致性覆盖报告 |

## 相关文档

- 同目录下的 `otel-genai-instrumentation-spec-v0.1.md`：基于本文内容进一步收敛出的可执行埋点规范（含分级要求、检查清单与变更台账）。