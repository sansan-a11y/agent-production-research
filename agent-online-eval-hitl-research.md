# Agent 结果在线评价与"低分转人工"专项调研：把"100% 准确"翻译成可承诺的工程目标

面向**平台团队**：如何为业务部门的 agent 建立"上线后持续评分 → 低分自动转人工 → 人工结论回流"的评价体系，并用**统计上可辩护**的方式承诺结果质量。

> 本文是前置两份调研的收敛篇：
> - [`agent-production-research.md`](agent-production-research.md)：L0–L9 生产化全景（其中 L4 是评测与门禁）
> - [`agent-runtime-supervision-research.md`](agent-runtime-supervision-research.md)：运行时监督（其中 7.2 是"采样在线评测"，7.6 是"指标 → 阈值 → 自动动作"）
>
> 本篇把那条最短路径单独摊开：**结果级评分 → 风险路由 → 人工复核 → 统计保证 → 审计证据**。
> 证据时间：2026-09-22。论文为 arXiv 实时检索（含 2026 年新作），产品为官方文档原文实测，法规为原文抓取。

---

## 与已有综述的关系：本篇在领域地图上的位置

已有综述已经画好地图，本篇不重复绘制，只填一个格子。直接对齐的三篇：

| 综述 | 它切的是哪一块 | 与本篇的关系 |
| --- | --- | --- |
| **2507.21504 Evaluation and Benchmarking of LLM Agents: A Survey** | 二维分类：**评什么**（agent 行为、能力、可靠性、安全性）× **怎么评**（交互模式、数据集与基准、指标计算方法、工具链）；并特别点出企业场景挑战：基于角色的数据访问、**可靠性保证（reliability guarantees）** 的需求 | 本篇是其"可靠性保证 + 企业落地"两个缺口的展开 |
| **2411.15594 A Survey on LLM-as-a-Judge** | judge 的方法、可靠性、标准化 | 本篇 §4 直接引用其后续的实证证据（翻转率、kappa、偏置）作为"judge 不能当唯一裁判"的依据 |
| **2606.04990 From Agent Traces to Trust: A Survey of Evidence Tracing and Execution Provenance in LLM Agents** | 轨迹溯源与执行出处 | 本篇 §8 的证据链与审计字段设计 |
| **2603.04445 Dynamic Model Routing and Cascading for Efficient LLM Inference: A Survey** | 路由与级联 | 本篇 §5.2 的级联选择性评估（便宜模型先判、必要时升级） |

**本篇只填一个格子**：把"评测结论"变成"在线生产决策"的那一段——

```
评分（含偏置与不确定性） → 阈值（带统计保证） → 低分转人工（含容量与疲劳约束）
      → 残余错误率的置信上界 → 审计证据链
```

这段在已有综述里通常被压缩成 "metric computation / tooling" 里的几行，但它恰恰是"agent 结果能否被承诺"的全部难点。

> **如果要把本篇改写成一篇 survey，建议骨架就是 §3–§8**：评什么（§3）→ 用什么评（§4）→ 分数如何变决策（§5）→ 人工如何运营（§6）→ 如何给保证（§7）→ 如何被审计（§8）。§0–§2 作为导言，§9–§12 作为工程落地与反模式。

---

## 0. 摘要：15 条核心判断

1. **"100% 准确"不能靠"把阈值调严"实现。** 评审模型本身是概率系统，实测证据：同一 judge 在同样输入上 pairwise 偏好平均翻转 **13.6%**（28% 的题目翻转率 > 20%，最高 56%）〔2606.13685〕；21 个 judge 经 kappa 校正后的一致性比 exact-match 低 **33–41pp**，judge 排名在不同 benchmark 之间最多移动 **14 位**〔2606.19544〕。阈值调严只是"更保守"，不是"不出错"。
2. **可承诺的目标是"零未复核出厂"（zero unvetted release），不是"零错误"。** 每条对外结果必须落到三类归宿之一：**确定性验证通过**、**评分达标自动放行（且有抽样审计覆盖）**、**人工复核签字**。这是 100% 可达成的，因为它是过程属性。
3. **把"100%"拆成三层，分别写指标**：① 复核覆盖率 = 100%（过程指标，可达成）；② 自动放行集合的**残余错误率 ≤ α**（结果指标，用抽样审计给置信上界）；③ **确定性可验证占比 ≥ v**（效率指标，唯一能靠工程投入持续提升、从而压低人工成本的那一层）。
4. **判据有强度阶梯，能用强判据就不要用弱判据**：程序化断言与可执行验证（编译/测试/状态回读/schema）→ 规则与接地检查（引用、检索证据、策略合规；AWS Bedrock Automated Reasoning 用形式逻辑来证明"输出与策略一致/矛盾"）→ 小模型校验器 → LLM-as-judge → 人工。**判据越弱，越要串联，越要人工兜底。**
5. **"结果正确"要定义在业务终态上，而不是文本上。** agent 的结果通常是数据库状态、工单、文件、付款、配置。把可验证目标锚到**世界状态**，能大幅提高强判据占比，直接减少人工队列压力。
6. **只看最终结果的在线评测会系统性漏掉"结果对、过程错"。** 实测：outcome-only judge 对"响亮故障"（用户可见）召回 **84%**，对"静默故障"只召回 **45%**，同时对 **33%** 的正确轨迹误报〔2609.00038〕。轨迹级与步骤级评分是在线评测的**必需项**。
7. **judge 必须被当作被监控对象**：监控它与人工标签的一致性（用 kappa，不用 exact match）、位置/顺序/长度偏置、重测稳定性、以及版本变更后的重基线。
8. **judge 同时是攻击面。** 知情的被评对象可用 prompt injection 稳定绕过多种 monitor 并在测试中完成恶意任务〔2510.09462〕——"监控模型是单点故障"必须进威胁模型。防御靠监控集成（集成增益取决于**技能**而非模型血统，〔2608.16190〕）、输入与判据隔离、以及**低分转人工 + 高分抽查**的双通道。
9. **"低分转人工"是有容量约束的统计决策问题，不是 if-else。** 三条理论基础：选择性预测/风险-覆盖曲线〔2603.15574 给出反例：高 OOD AUROC 不保证安全弃权，50% 覆盖率下风险仍 99.6%〕、**conformal 阈值校准**〔2208.02814、2512.12844、2403.01216〕、**learning-to-defer**〔成本敏感+容量约束 2403.06906；免训练 2509.12573；疲劳感知 2604.00904〕。
10. **推荐落地策略："三区间 + 双保险"。** 红区（score < τ_low）不放行；黄区必走人工；绿区自动放行但保留 **ε 比例随机审计**。ε 不能为 0——"只审低分"永远发现不了"judge 与被评对象一起错"的共谋失败。
11. **转交会引入偏置，必须监控"人工看到的样本分布"。** 标准 learning-to-defer 策略存在**类依赖采样偏置**，少数类被过度转交，并进一步诱发人工偏见（用户研究 N=226）〔2608.28050〕。人工看的是有偏样本，长期会把偏见固化进数据集。
12. **人工复核必须防锚定、防橡皮图章。** 实测：AI 建议会让专家把原本正确的判断改错，**自动化偏见率 7%**（n=28 病理专家）〔2411.00998〕。产品侧已有对应机制：Braintrust 的 **blind reviews**、LangSmith 的"评审人看不到彼此的反馈"、Langfuse 明确要求"如果自动检查是错的，直接说它错了，**不要为了迁就它把应用判错**"。
13. **人工也要被审计**：金标夹带 + 每位评审人的准确率/敏感度 + 标注者间一致性（Cohen's κ / Krippendorff α）+ 分歧仲裁 + 疲劳与负荷监控。理由：现有 L2D 方法"假设人力性能静态"，与疲劳实验证据相矛盾〔2604.00904〕。
14. **保证必须写清作用域，而且会过期。** 任何"VALID/达标"都只在其判据覆盖范围内成立——AWS Automated Reasoning 自己就明确写出"只保证被策略变量捕获到的部分"；分布漂移下 conformal 覆盖会失效（欠覆盖）〔2510.05566〕。因此必须**定期重校准 + 覆盖率监控 + 越界自动收紧**（fail-closed 退化）。
15. **对领导/客户/监管的表达是"审计算术"，不是形容词。** 要声称自动放行集合残余错误率 ≤ 1%（95% 置信），至少要抽检约 **300 件**零错误样本（rule of three: n ≈ 3/α）；≤ 0.1% 需要约 **3000 件**。**"100% 准确"的报价单就是审计预算。** 国内监管要求的是同一个动作：《生成式人工智能服务管理暂行办法》第八条要求"开展数据标注质量评估，**抽样核验标注内容的准确性**"。

---

## 1. 先修正目标：把"100% 准确"拆成三层可验证的承诺

### 1.1 三个"100%"，只有一个能写进 SLO

访谈里最危险的一句话是"我们要保证 agent 结果 100% 准确"。工程上必须先说清楚，这里的 100% 指的是哪一个：

| 层级 | 说法 | 能否 100% | 度量方式 | 谁来保证 |
| --- | --- | --- | --- | --- |
| P0 | **自动判定准确率 100%**（评测器从不出错） | **不能** | 不可度量（定义自相矛盾） | —— |
| P1 | **复核覆盖率 100%**（zero unvetted release） | **能** | 出厂结果中"未经过任何判据或人工"的条数 = 0 | 平台流程 |
| P2 | **自动放行集合残余错误率 ≤ α** | 能，但需抽样 | 对绿区做随机审计，给出错误率的置信上界 | 统计 + 审计预算 |
| P3 | **确定性可验证占比 ≥ v** | 能，且可持续提升 | 强判据覆盖的流量占比 | 业务 + 平台共建 |

**结论**：把"100% 准确"落成 P1（100%，过程属性）+ P2（≤ α，统计属性）+ P3（≥ v，效率属性）。三句话写进上线协议：

1. "所有对外交付的 agent 结果，100% 具备可追溯的裁决记录（判据结果或人工签字）。"
2. "自动放行子集的残余错误率不高于 α，每季度用随机抽样审计复验并出具置信区间。"
3. "强判据（可执行验证）覆盖率不低于 v，且每季度至少提升 x 个百分点或解释为何不能。"

### 1.2 为什么"零未复核出厂"是唯一可承诺的目标（三条硬理由）

**理由一：判据本身不可靠，且误差不可完全消除。** LLM-as-judge 的实测可靠性远比直觉差：
- 同样的输入重复评测，pairwise 偏好翻转率平均 13.6%，最高 56%；pointwise 分数差异小且不显著，形成 **pairwise–pointwise gap**（judge 经常"选出一个赢家"，尽管它自己打的分并不支持）〔2606.13685〕。
- judge 的"可靠性(reliability)"与"有效性(validity)"是两件事：重测信度 > 0.95 的 judge 可以同时存在 > 0.10 的位置偏置〔2606.19544〕。
- 判分本身有系统偏置：**rubric 顺序偏置、score ID 偏置、参考答案分数偏置**〔2506.22316〕；rubric 生成质量（覆盖不足、维度混淆、偏好方向错、冗余）会直接降低 judge 准确度〔2602.05125〕。

**理由二：模型会漂移，且"检测到异常"不等于"能安全弃权"。** 域漂移下标准的 conformal 覆盖会失效（欠覆盖），需要按测试样本邻近度对校准样本重加权才能保持有效性〔2510.05566〕；更刺眼的反例是：即使 OOD 检测 AUROC ≥ 0.91，选择性分类在 50% 覆盖率下风险仍可达 99.6%——**"能发现分布外"不保证"弃权后剩下的就对"**〔2603.15574〕。

**理由三：攻击面存在。** 当被评对象知道监控协议与监控模型时，可以用公开的或零样本的 prompt injection 让多种 monitor 稳定失效〔2510.09462〕。只要系统里有"自动放行"这个动作，就必须假设它会被针对性优化（无论是人的恶意还是模型的 specification gaming）。

**所以：自动判定用来"分层和提速"，人工复核用来"兜底和签字"，两者缺一不可。**

### 1.3 判据强度阶梯：四类判据，先强后弱

| 级别 | 判据类型 | 典型手段 | 证据强度 | 适用 | 主要陷阱 |
| --- | --- | --- | --- | --- | --- |
| **L-A** | 可执行/确定性验证 | 编译、单测、schema/类型校验、数据库状态回读、幂等重放、diff 比对、形式化/自动推理（AWS Automated Reasoning） | 最强，可给"证明" | 有客观终态的任务 | 只保证规则覆盖到的部分（AWS 原文明确声明此局限） |
| **L-B** | 规则与接地检查 | 引用必须命中检索片段、工具返回必须支持结论、策略/合规规则、必含/必禁串、MiniCheck 类小模型校验 | 较强，可审计 | 有证据源的任务 | 规则易被"贴标签"绕过；覆盖有限 |
| **L-C** | 概率判据（LLM-as-judge / reward model） | rubric 打分、二分类判定、举证式判定、集成/自一致性 | 弱，有偏且可漂移 | 主观/开放语义维度 | 见 §4：偏置、翻转、可被注入 |
| **L-D** | 人工复核 | 领域专家按 rubric 打分 + 修正稿 | 结论性（仍需被审计） | 高风险、不可验证、分布外 | 锚定效应、橡皮图章、疲劳 |

**四条使用规则**：
1. **L-A 优先**：能写成断言的就别交给 judge。这是降低人工成本最根本的手段（也是唯一能让 P3 上升的手段）。
2. **L-C 只用于 L-A/L-B 覆盖不到的语义维度**，且必须校准（§5.2）。
3. **安全类判据必须是否决式（veto），不能进加权平均**——否则"高危项不过"会被"其它项高分"稀释掉。
4. **判据无法判定时（error/skipped/超时），默认动作必须是"转人工"，不是"放行"**。这是 fail-safe 的第一原则，也是最常被写反的地方。

### 1.4 目标改写模板（可直接抄进上线评审）

> 本 agent 的结果准确性目标不是"零错误"，而是：**零未复核出厂**。
> - 每条结果的裁决路径（强判据结果 / 评分与阈值 / 人工签字）100% 落库可追溯。
> - 自动放行子集残余错误率目标 ≤ α=___%（按业务损失定），每季度以 n=___ 的随机审计样本复验，报告 Wilson 95% 置信上界。
> - 强判据覆盖率基线 v=___%，季度提升目标 ___。
> - 命中"必须转人工"清单（§6.1）的结果，不得自动放行，与分数无关。

---

## 2. 系统全景：在线评价 + 转人工的九段流水线

### 2.1 九段流水线

```
① 事件与轨迹采集   →  ② 触发与采样  →  ③ 判据执行（L-A→L-B→L-C）
      ↑                                                    ↓
⑨ 门禁与发布 ←  ⑧ 回归与数据集  ←  ⑦ 标签回流  ←  ⑥ 人工复核队列
                                                           ↑
                                     ⑤ 风险路由（三区间+双保险）
                                                           ↑
                                              ④ 分数校准与阈值
```

| 段 | 职责 | 关键工程点 | 常见错误 |
| --- | --- | --- | --- |
| ① 采集 | 拿到"结果"与"过程"的完整证据 | 用 OTel GenAI 语义约定统一 trace（见 [`otel-genai-instrumentation-spec-v0.1.md`](otel-genai-instrumentation-spec-v0.1.md)）；必须记录**最终业务终态**与工具原始返回 | 只存文本答复，不存状态变更，导致"结果定义"落在文本上 |
| ② 触发与采样 | 决定评什么、评多少 | 分层采样：高风险全量 + 其余按比例；支持按 filter 触发（LangSmith 可按 run 过滤 + sampling rate，例 10% → `0.1`） | 全量跑 judge → 成本失控；或只跑 1% → 尾部风险完全看不见 |
| ③ 判据执行 | 由强到弱串联 | L-A 不通过直接进红区，不浪费 judge 调用；judge 用结构化输出（Phoenix 用 tool calling 强制返回 enum + explanation） | 用自由文本解析判分结果 |
| ④ 校准与阈值 | 把原始分变成可比、可解释的决策变量 | 用留出校准集做分位数定阈（§5.2）；记录 judge 版本、prompt 版本、温度 | 阈值拍脑袋；judge 升级不做重基线 |
| ⑤ 风险路由 | 三区间 + 双保险 + 硬触发 | 见 §5.4、§6.1 | 只用单一阈值二分区 |
| ⑥ 人工队列 | 排队、分配、评审、仲裁 | 见 §6.2–§6.5 | 队列 FIFO、无 SLA、无金标、无盲评 |
| ⑦ 标签回流 | 人工结论 → 结构化标签 | 修正稿进数据集（Langfuse corrections `dataType=CORRECTION` + diff；Braintrust 可写 `expected` 字段） | 人工标签直接当金标，不做一致性检查 |
| ⑧ 回归 | 线上问题 → 离线用例 | 线上失败样本自动入离线数据集（LangSmith「Add to Dataset」；Braintrust Topics/Patterns → 数据集/评分器/复核任务） | 线上发现的问题不进回归，重复踩坑 |
| ⑨ 门禁与发布 | 通过条件绑定版本 | 判据、阈值、数据集、judge 版本与 agent 版本**原子绑定**（承接前篇 L5/L8） | 只发 agent，不发判据版本 |

### 2.2 三条"必须存在"的旁路

这三条旁路决定了这套体系能否真正兜住"100%"，缺一条就会留下系统性盲区：

1. **随机审计通道（audit budget）**：对绿区（自动放行）按 ε 比例随机抽检，**不按分数挑选**。目的不是抓错，而是**持续估计绿区的残余错误率**——这是 P2 指标的唯一来源。只审低分样本时，残余错误率在数学上不可估计。
2. **金标夹带通道（gold-seeded）**：把已知正确答案的样本混入黄区队列，用来度量**评审人**而非被评对象；同时监控评审人分歧（§6.4）。
3. **用户与业务信号通道**：用户负反馈、投诉、退款、回滚、人工返工（Langfuse `user feedback`、LangSmith「attach user feedback」）——这是唯一能捕获"judge 与被评对象一起错"的信号源，优先级高于 judge 分数。

### 2.3 与埋点规范的接口

本流水线对埋点的最小要求（在前篇 T0/T1/T2 分级基础上增补）：

- **结果标识**：`result_id`（幂等键）、agent 版本、判据版本、judge 版本、prompt 版本、采样决策（是否被抽中、命中了哪条规则）。
- **裁决记录**：每条判据的原始输出、结构化分数、`verdict`（pass/fail/uncertain/error）、阈值快照。
- **人工记录**：评审人 ID（或角色）、评审时间、耗时、是否盲评、是否被修正、是否与 judge 不一致。
- **终态证据**：业务对象 ID 与状态快照的引用（能在审计时回放）。
---

## 3. 评什么：把"结果"锚定在世界状态上

### 3.1 三个层次的目标
| 层次 | 评价对象 | 判据类型 | 说明 |
| --- | --- | --- | --- |
| 制品层 | 生成的代码、文档、SQL、配置、报表 | L-A + L-B | 最容易被自动验证 |
| 轨迹层 | 工具选择、参数、顺序、依赖、是否走了弯路 | L-A（可重放）+ L-C（步骤 rubric） | 决定"静默故障"能否被抓到 |
| 终态层 | 数据库/工单/文件/付款/权限的最终世界状态 | L-A | **最应该成为"结果"的定义** |

**原则**：能定义在终态层的，不要退到制品层；能定义在制品层的，不要退到纯文本。越靠上，L-A 占比越高，人工队列越轻。

> 这一分层不是本文的发明，而是当前研究的共识：证据追溯与执行出处（execution provenance）方向的综述明确指出——**最终答案的准确率本身无法解释输出是如何产生的、每条断言由什么证据支撑、工具调用是否合理、记忆如何影响后续决策、失败发生在哪一步**〔2606.04990〕。这正是"只评最终结果"会漏掉静默故障〔2609.00038〕的根本原因。

配套证据（对 agent 轨迹评测的专门度量）：
- **TRAJECT-Bench**：除最终准确率外，单独报告工具选择正确性、参数正确性、依赖/顺序满足度——把"轨迹质量"拆成了可诊断指标〔2510.04550〕。
- **ATLAS**：双层诊断——请求级（trajectory）信号定位缺陷发生的执行位置与能力维度，交互级（user-wise）信号判断后续服务是否仍与上文一致〔2608.30685〕。
- **Agentic Continuous Evaluation of Skills**：企业 agent 进生产后，评测对象应从"agent"上移到可复用的**技能/工具/工作流包**，并配套可复核的证据〔2608.20614〕。

### 3.2 可验证性分级与"验证作用域"
给每个 agent 任务标注**可验证性等级**，直接决定路由上限：

| 等级 | 判据 | 自动放行上限 |
| --- | --- | --- |
| V1 完全可判定 | 断言/测试/状态比对全通过 | 可放行（无需 judge） |
| V2 部分可判定 | 硬断言过 + judge 过 | 可放行（需校准 + 抽查） |
| V3 仅可评审 | 只有 judge/人工 | 需人工签字或高频抽查 |
| V4 不可验证（主观/长周期） | 事后业务指标 | 默认人工；或改为"建议模式"（agent 出草稿，人决策） |

**验证作用域声明**（必写）：任何"通过"都只在其判据覆盖的范围内成立。AWS Automated Reasoning 的官方说明就是最佳范例：它明确列出"只验证策略变量捕获到的内容、不防注入、不做过题判定、不支持流式、仅英文"〔vendor3/aws-autoreasoning〕。**把自己的局限性写出来，是让保证可信的前提，而不是示弱。**

### 3.3 没有参考答案时怎么评（在线评测的常态）
在线评测天然缺 ground truth。可用的替代物，按强度排序：
1. **可执行终态**（最强）：任务是否真的完成。
2. **证据接地**：答复中的每个事实性断言必须有检索片段或工具返回支撑；无支撑即判 uncertain（→ 转人工），而不是判 pass。
3. **约束合规**：策略/合规规则集（可参考 Bedrock Automated Reasoning 的"策略→形式化规则→验证"范式）。
4. **一致性**：多次独立生成/多次判定的自一致性；低一致性 → 转人工（自一致性集成在 trajectory judge 中被列为一种手段〔2609.00038〕）。
5. **rubric 打分**（最弱）：必须校准、必须与人工标签对齐后再上线（§4.4）。

### 3.4 判据合流：硬否决 + 分级 + 不确定态
推荐的多判据合流规则（避免"平均分掩盖高危项"）：

```
决定 = 红区  若 (任一 veto 判据 fail) 或 (任一判据 error/uncertain 且不可自动重试)
     = 黄区  若 (加权分 < τ_high) 或 (命中 §6.1 硬触发清单)
     = 绿区  否则（且保留 ε 随机审计标记）
```
三个设计要点：
- **veto 判据独立于分数**：安全、合规、权限、金额阈值一旦触发，分数再高也不放行（Azure 的 agent evaluators 就以 **Pass/Fail** 二元输出为主，而不是把一切压成分数〔vendor3/azure-agent-evaluators〕）。
- **不确定态必须显式建模**。Giskard 的结果表就把场景分成 Pass/Fail/**Error**/**Skipped**，并明确 Skipped 通常是"缺少必需检查或标注"〔vendor3/giskard-eval-review〕。工程上要把 `error`/`skipped` 与 `fail` 分开统计——它们的运维含义完全不同（前者是产能问题，后者是质量问题）。
- **分数只用于排序与阈值，不用于对外承诺**。"平均分 4.6/5"不是准确率。

---

## 4. 评分器可靠性：LLM-as-judge 的能力边界（实测证据）
本章是全文的"恐惧清单"，目的是让阈值设计者知道自己在跟什么打交道。**不读完本章就定阈值，等于在不知道量具误差的情况下做精加工。**

### 4.1 一致性：翻转率与 kappa 虚高
| 现象 | 实测数值 | 来源 |
| --- | --- | --- |
| 同一题目重复 pairwise 评测的偏好翻转 | 平均 **13.6%**；28% 题目 > 20%；最高 **56%** | 2606.13685 |
| 首位偏置 | GPT-4o-mini 72% 选 A（p=0.024） | 2606.13685 |
| pairwise 与 pointwise 的结论冲突 | pointwise 分差小且不显著，pairwise 却"选出赢家" | 2606.13685 |
| exact-match 高估一致性 | 与 Cohen's κ 相差 **33–41pp**（MT-Bench） | 2606.19544 |
| 排名不稳定 | 同一批 judge 在不同 benchmark 间排名最多移动 **14 位** | 2606.19544 |
| 信度与偏置并存 | 重测信度 > 0.95 的 judge 同时有 > 0.10 位置偏置 | 2606.19544 |
| 估计量偏差 | 朴素 judge 输出系统性有偏；跨模型共享校准可导致结论符号反转 | 2605.06939 |

**工程结论**：
- **一致性指标用 kappa/α，不用 exact match**；对 judge 做"重复评测稳定性"验收（同一输入跑 k≥3 次，报告翻转率）。
- **pairwise judge 不适合做在线质量门禁**（翻转率太高）；在线门禁优先用 **pointwise + 结构化 rubric + 阈值**，pairwise 留给离线 A/B 与人机对齐。

### 4.2 系统性偏置清单（定阈值前逐条排除）
| 偏置 | 表现 | 缓解 |
| --- | --- | --- |
| 位置偏置 | A/B 顺序影响结论 | 交换位置取平均；顺序随机化 |
| 冗长/格式偏置 | 偏好更长、更"像样"的答案 | 长度控制（Length-Controlled 类做法）、格式剥离后再评 |
| 自偏好 | 偏好自己家族的输出 | 换 judge 家族；跨家族集成 |
| rubric 顺序偏置 | rubric 条目顺序改变分数 | 顺序随机化；条目独立打分 |
| score ID 偏置 | 分数标签（如 "1" vs "5"）本身影响结果 | 用语义标签而非裸数字；枚举顺序随机化 |
| 参考答案分数偏置 | 有参考答案时分数被"拉"向参考答案 | 无参考场景下不要给参考答案 |
| 立场/长度/主题漂移 | 不同主题下判据可信度不同 | 分主题校准；分主题设阈 |

来源：偏置类型与 rubric 生成问题〔2506.22316、2602.05125、2602.09383〕。

### 4.3 结构化输出与判定协议（提高可复现性）
- **用工具调用强制结构化输出**：Phoenix 的 evaluator 会为判定结果生成一个工具（如 `{label: enum[correct, incorrect], explanation: string}`），强制模型调用它而非自由文本，再映射为分数〔vendor3/phoenix-evals〕。
- **举证式判定**：要求先给证据（引用片段/工具返回）再给分数；Langfuse 的 judge 输出 `score + reasoning`，Weave 的信号返回 `confidence + reasoning`〔vendor3/lf-llm-as-a-judge、vendor3/weave-monitors〕。
- **锁定 rubric 规范并把信号校准到人类分数边界**：Rulers 类做法（任务级 rubric 冻结 + 证据接地 + 校准到人类打分边界），并在语义等价扰动下更稳定〔2601.08654〕；把政策/rubric 当作"测量规范"来写〔2605.30803〕。
- **rubric 本身要工程化**：递归分解-过滤-去相关加权，避免覆盖不足/维度混淆/冗余〔2602.05125〕。Google 的 Gen AI evaluation service 把 rubric 分为 **adaptive rubrics（每个 prompt 生成专属 pass/fail 判据，官方推荐）** 与 **static rubrics**〔vendor3/vertex-eval〕。

### 4.4 judge 必须是"被监控对象"：四项 SLO
| 指标 | 定义 | 建议阈值（示例） |
| --- | --- | --- |
| 人机一致率 | judge 与人工标签在黄金集上的 κ | κ ≥ 0.6，且分主题无显著塌陷 |
| 重测翻转率 | 同输入重复评测结论改变比例 | ≤ 5% |
| 位置偏置 | 交换顺序后结论改变比例 | ≤ 5% |
| 误报/漏报 | 对正确轨迹的误报率、对静默故障的漏报率 | 误报 ≤ 10%；**静默故障漏报必须有专项探针**（见 §4.5） |

judge 的**上线流程**应是：写 rubric → 与人工标签对齐（Agreement）→ 测偏置与稳定性 → 冻结版本 → 才能进生产门禁。Langfuse 的 annotation queue 文档把"**Align your LLM-as-a-Judge evaluation with human annotation**"直接写成了队列的用途之一〔vendor3/lf-annotation-queues〕。

### 4.5 静默故障：最需要"低分转人工"去接的那一类
outcome-only judge 的核心盲区实测〔2609.00038，400 条轨迹、5 类 judge、含确定性工具环境与故障注入〕：
- 对**响亮故障**（用户可见）召回 **84%**；
- 对**静默故障**（结果看起来对、过程错了）召回仅 **45%**；
- 同时把 **33% 的正确轨迹**误报为有问题。

**推论（本文最重要的设计结论之一）**：
1. 在线评分**必须同时包含步骤/轨迹级判据**，否则静默故障类问题的检出率不足一半。
2. 静默故障的"低分"信号本来就很弱——**不能指望 judge 把它挑出来送进人工队列**；必须靠**随机审计**和**业务终态验证**去兜。
3. judge 的高误报率意味着"低分转人工"里会混入大量假警报：队列必须有**分诊（triage）**层，否则人工会被噪声淹没、进而橡皮图章化。

### 4.6 judge 也是攻击面
- 知道协议与监控模型的被评模型，用公开/零样本 prompt injection 即可稳定绕过多种 monitor，并在两个测试环境中完成恶意任务〔2510.09462〕。
- 监控集成有帮助，但增益来源是**监控者的技能**而非"模型血统多样性"：24 个开源 monitor（9 个预训练谱系，pAUC@10%FPR 0.028–0.803）的实测显示，用相关性最小化来挑选面板并不能预测面板效果〔2608.16190〕。
- reward/judge 的过优化可被集成缓解，但 benchmark 与最终策略性能相关性弱——**不要用"judge 分数上升"当作"质量上升"的证据**〔2310.02743、2505.12763〕。

**防御要点**：判据与输入隔离（judge 不应把被评内容当指令）、对判据输入做注入检测、监控集成按技能选型、以及最根本的——**自动放行永远配一条人工抽查通道**。

---

## 5. 分数 → 决策：把阈值变成有保证的统计决策

### 5.1 风险-覆盖曲线：把"阈值"翻译成"代价"
设覆盖率为 c（自动放行比例），风险 R(c) 为放行集合上的错误率。任何阈值策略都对应曲线上的一个点：
- **人工负载** = (1 − c) × 流量；
- **残余风险** = R(c)。

于是"低分转人工"就是：**在人工容量约束下最大化 c，或在风险约束下最小化人工负载**。没有这个视角，阈值讨论只会变成"我觉得 0.8 比较安全"。

反例警示：**高的 OOD 检测能力不保证安全弃权**——某研究在严重域漂移下发现，即使 OOD AUROC ≥ 0.91，选择性分类在 50% 覆盖率时风险仍高达 99.6%〔2603.15574〕。**"我们能检测到分布外"≠"我们的阈值在分布外也安全"。**

### 5.2 用 conformal 校准阈值（给阈值一个有限样本保证）
做法（split conformal / risk control）：
1. 取一批**带人工标签的校准样本**（n 条）；
2. 对每条算非一致性分数（judge 分数、不确定性、多判据组合）；
3. 取分数分布的**上分位数**作为阈值 τ，使在名义置信 1−α 下，放行集合的错误率 ≤ α（miscoverage 的典型有限样本界为 α + 1/(n+1) 量级）；
4. 上线后**监控覆盖率与实测风险**，定期用新标签重校准。

相关工作与可复用结论：
| 方法 | 解决什么 | 来源 |
| --- | --- | --- |
| Conformal Risk Control | 把 conformal 从"覆盖率"扩展到**任意单调损失的期望**（如 FNR、token-F1），且样本效率最优至 O(1/n) | 2208.02814 |
| Selective Conformal Risk Control | 两阶段：先选置信样本、再在选中子集上做风险控制，兼顾集合效率与保证 | 2512.12844 |
| API-only conformal | 没有 logits、只能调 API 的模型也能拿到用户指定覆盖率的保证 | 2403.01216 |
| 域漂移感知 conformal | 漂移下标准 conformal 会欠覆盖 → 按样本邻近度重加权校准样本 | 2510.05566 |
| 自适应 conformal（事实性） | prompt 级自适应，支持"过滤不可靠断言"的选择性预测 | 2604.13991 |
| Trust or Escalate | **选择性评估 + 人类一致性的可证明保证**：按"用户指定的一致水平"决定何时信任 judge；用 Simulated Annotators 提升 judge 校准以提高覆盖率；并用**级联选择性评估**（便宜模型先判、必要时升级） | 2407.18370 |

> **Trust or Escalate 是最贴近本文命题的单篇工作**：它把"要么信、要么升级给人"做成了一个带保证的框架，并给出了"提高 judge 校准 → 提高可覆盖比例"的因果链。落地时建议直接照抄它的三段结构：校准 → 覆盖率估计 → 级联升级。

### 5.3 Learning-to-Defer：人工是有成本、有容量、会疲劳的资源
"低分转人工"在机器学习文献里就是 **learning to defer (L2D)**。三条必须吸收的工程结论：
1. **成本敏感 + 容量约束**：DeCCaF 框架处理了"I 类/II 类错误代价不同 + 人力工作容量有限 + 训练集只要求每个实例一个专家预测"〔2403.06906〕。这直接对应我们的两条现实：漏放行的代价 ≠ 误放行的代价；人工带宽有限。
2. **免训练、可换专家**：用 conformal 预测集 + 专家区分度准则来选最合适的专家，无需为每个专家重新训练〔2509.12573〕。适合"评审人是流动的/多技能池"的组织。
3. **疲劳与负荷是状态变量**：现有 L2D 假设人力性能静态，与疲劳的实验证据矛盾；FALCON 用疲劳曲线 + 约束马尔可夫决策过程，在"人机协作预算"下优化准确率，并给出 FA-L2D 基准〔2604.00904〕。**队列长度会改变人工准确率**，因此队列不是无底洞，容量规划属于模型的一部分。
4. 专家标注可以少采样：三步法可显著减少训练 L2D 所需的专家预测数量〔2304.07306〕；需要合成专家池做实验时可用 FiFAR（含 50 名合成分析师，偏见各异）〔2312.13218〕。

### 5.4 推荐策略：三区间 + 双保险（含算例）
```
红区  score < τ_low            → 不放行：拒答 / 重跑 / 换模型 / 强制人工
黄区  τ_low ≤ score < τ_high   → 必走人工队列（可再加分诊层）
绿区  score ≥ τ_high           → 自动放行 + 随机审计抽样（ε）
旁路  命中硬触发清单（§6.1）    → 与分数无关，直接进人工
旁路  判据 error/uncertain      → 直接进人工（fail-safe）
```
**算例**（工程估算，非实测）：日流量 N=20,000 条；判据成本 0.02 元/条；人工 3 分钟/条、人力成本 1.5 元/分钟。
- 方案 A（两区间，只审低分）：黄区 8% → 1,600 条/日 → 人工 4,800 分钟/日（≈ 10 人·日）。绿区无审计 → **残余错误率不可估计**。
- 方案 B（三区间 + 双保险）：黄区 8% + 绿区审计 ε=2% → 1,600 + 368 = 1,968 条/日（≈ 12.3 人·日）。多花 23% 人力，换来"绿区残余错误率可估计、可承诺"。

**这笔账就是本文的核心取舍**：**不敢对自己抽样，就没有资格说 100%。**

### 5.5 两个反直觉风险（必须监控）
1. **转交偏置**：标准 L2D 策略存在类依赖的采样偏置，会**过度转交少数类**；且这种不对称转交会诱发人工自身的偏见，导致下游决策变差（用户研究 N=226）〔2608.28050〕。
   → 监控"进入人工队列的样本在业务维度上的分布"是否与整体分布一致；对少数类/边缘人群单独设阈或按比例兜底。
2. **人工只看低分样本 → 人工的能力被窄化**：评审人长期只见异常样本，会失去对正常样本分布的判断力，且更容易被 judge 的理由锚定。→ 金标夹带 + 随机审计样本混入队列（§6.4）。

---
## 6. 转人工：把它当"运营系统"，不是"一个按钮"

### 6.1 触发矩阵：什么时候必须转人工（与分数无关）
| 触发类 | 具体条件 | 理由 |
| --- | --- | --- |
| 高危动作 | 支付/退款/转账、外发消息、删除/覆盖数据、改权限、签署、医疗/法律/金融建议 | 后果不可逆或高损失 |
| 不可逆性 | 任何无法用回滚补偿的动作 | 错误不可挽救 |
| 判据不确定 | judge 返回 uncertain/error/超时、多判据冲突、自一致性低 | fail-safe |
| 冷启动 | 新工具/新版本/新 prompt 的**首批样本**、新用户群、新业务场景 | 无历史基线 |
| 分布外 | 新意图聚类、异常输入长度/语言/格式、Arize Signal / Braintrust Topics 类自动聚类异常 | 阈值在 OOD 下不成立（§5.1） |
| 用户侧信号 | 负反馈、投诉、重复追问、要求转人工 | 最强信号源 |
| 业务侧信号 | 返工、退款、回滚、客诉工单关联到该结果 | 事后兜底 |
| 随机审计 | 按 ε 抽样命中（分层：高风险层抽样率更高） | 估计残余错误率 |
| 金标夹带 | 预置已知答案的样本混入 | 度量评审人 |
| 安全否决 | 注入检测命中、策略违规、敏感数据外泄迹象 | 独立于分数 |

### 6.2 队列机制：产品实测能力对照（2026-09）
| 产品 | 队列/复核形态 | 值得抄的细节 |
| --- | --- | --- |
| **LangSmith** | Annotation Queues（单条 + **Pairwise 队列**）；rubric/instructions；**reservations**（限时锁定，超时释放）；多评审人阈值或指定评审人；队列三态（Needs Review / Needs Others' Review / Completed）；**评审人看不到彼此反馈**；`Add to Dataset`；assertions 记录验收条件 | 在线 evaluator 支持 **filter + sampling rate**（例 10% → `0.1`）、可回填历史、可设**周花费上限**（超限自动暂停）；在线 evaluator 命中会把 trace 升级为长保留期（**注意成本影响**） |
| **Langfuse** | Annotation Queues（按 score config 定义维度）；批量/单条入队；trace/observation/session 三级；键盘驱动（→/← 切换、1–9 选档、⌘↵ 完成并进入下一条）；支持写入**修正稿**（corrections，`dataType=CORRECTION` + diff 视图） | 文档明确写出评审人动作规范："**如果自动检查是错的，直接说明它错了，不要为了迁就它把应用判错**"；修正稿**不会**自动更新数据集，需人工确认新数据集版本；保留原始 run/case id 做溯源；在线 evaluator 推荐落在 **Observation 级**，trace 级已弃用 |
| **Braintrust** | Human review：categorical / continuous / free-form 分数；free-form 可写 `expected` 字段（含嵌套路径）；**blind reviews**（评审人互不可见）；`Require all scores`；按条件显示分数（如"仅当分诊分超阈时显示详细 rubric"）；多评审人分数合并 | "分数可见性"是**显示过滤器，不是权限边界**——文档自己提示可用 `Show all scores` 绕过；真正的权限要走项目权限。这个坦白值得引用：**别把 UI 特性当合规控制** |
| **Arize（Phoenix / AX）** | 客户端 SDK + 服务端 UI 两套评测；确定性 evaluator 与 LLM-judge 并存；judge 用 tool calling 结构化输出并默认返回 explanation；AX 侧 **Signal** 自动把反复出现的失败聚成排序问题（含 trace 证据与修复建议） | 用"自动聚类 + 排序"替代人工翻日志找问题 |
| **Opik** | Annotation Queues（面向 SME 的复核与标注）；REST API 提供队列项的**加锁/续锁/查询活跃锁/批量入队** | 队列锁有独立 API → 适合自建工具时的并发控制范本 |
| **W&B Weave** | Monitors + **13 个内置信号**（Hallucination / Low quality / User frustration / Jailbreaking / NSFW / Lazy / Forgetful + Network / Ratelimited / Request Too Large / Bad Request / Bad Response / Bug）；LLM-as-judge 二分类，同组信号一次调用批量判，返回 **confidence + reasoning**；可对信号阈值告警 | 内置信号把"质量"和"错误"分开建模（Quality vs Error），可直接映射到本文的红/黄/绿 |
| **Giskard Hub** | 协作标注 + 连续红队 + **cron 调度评测**；场景可重复跑 1–5 次（首次失败即停）；结果分 Pass/Fail/Error/Skipped；**hidden 结果不计入指标**；按 check / failure category / tag 统计失败率 | "Skipped = 缺必需检查或标注"这个显式状态，正是我们把"不确定"单列的原因 |
| **Galileo（2026-08-07 起为 Splunk Agent Observability）** | Metrics / Signals / Annotations / Agent Control；Luna-2 与 **Autotune** 用于改进 LLM-judge；支持 cron 调度评测 | 老客户与新客户的文档域名已分离，选型时注意 |
| **AWS** | AgentCore Evaluations（内置/第三方/自定义 evaluator，LLM-as-judge，trace 统一格式，接入 OTel / OpenInference；含 **Online evaluation**、on-demand、batch、dataset、simulation）；Bedrock Guardrails **Automated Reasoning**（形式逻辑验证）；A2I（通用人工审核工作流服务） | AgentCore 默认每账号每 region 最多 1,000 个评测配置；★ 注意：AWS 文档站为 JS 渲染，本文未能取到 online evaluation 子页正文（见 §13 取证说明）|
| **Azure AI Foundry** | Agent evaluators：Task Completion / Customer Satisfaction(1–5) / Task Adherence / Task Navigation Efficiency / Intent Resolution，输出 **Pass/Fail** 或按阈值转二元；**连续评测按采样率**评估生产流量并进 Observability 仪表盘；另有按测试集**定时评测**发现漂移、**定时红队**、Azure Monitor 告警 | 生产后监控四件套（运行指标 / 采样连续评测 / 定时集评测 / 定时红队）可直接作为验收清单 |
| **Google Vertex** | Gen AI evaluation service：adaptive rubrics（推荐）/ static rubrics / computation-based / 自定义 Python 函数；数据集可从**生产日志采样**或合成生成；支持 agent 评测（agent traces + 响应质量） | "adaptive rubrics"= 每个 prompt 生成专属 pass/fail 判据，适合长尾场景 |
| **开源标注** | Label Studio（28.3k★）、Argilla（面向 AI 工程师与领域专家共建数据集，支持文本/RAG/偏好/多模态）、doccano、cleanlab（标签质量） | 需要私有化/自建队列时的主要底座 |

### 6.3 三条硬规则
1. **盲评（blind review）**：默认**不向评审人展示 judge 的分数与理由**，避免锚定；需要提示时，先让评审人独立打标，再揭示 judge 结论并记录分歧。
   - 证据：AI 建议会让专家把原本正确的判断改错（自动化偏见率 7%）〔2411.00998〕；产品侧 Braintrust 提供 blind reviews、LangSmith 让评审人互不可见、Langfuse 明确要求"不要为迁就自动检查而改判"。
2. **人工也要被审计**：金标夹带 → 每位评审人的准确率与敏感度；同一批样本多评审人 → Cohen's κ / Krippendorff α；分歧 → 第三方仲裁并回写规则；负荷 → 队列长度与个人吞吐与准确率的关系（疲劳，〔2604.00904〕）。
   - 反例风险：若把人工标签直接当金标训练 judge，会形成"人工偏见 → judge 偏见 → 更多偏见样本"的闭环。
3. **随机审计不可省**：绿区按 ε 随机抽检，且**抽样框覆盖全部绿区**（不是只抽"看起来可疑"的）。这是残余错误率唯一可估计的来源，也是 AI control 文献里 audit budget 的工程对应物。

### 6.4 队列的运营参数（建议在平台层统一定义）
| 参数 | 建议 | 说明 |
| --- | --- | --- |
| 优先级 | 按 **风险 × 不可逆性 × 金额/影响面** 排序，不 FIFO | 高危先看，避免"积压把大额工单压在后面" |
| SLA | 黄区分级 SLA（高危 ≤ 1h，普通 ≤ 24h） | SLA 违约要告警，而不是静默堆积 |
| 单件预算 | 3–10 分钟；超时自动升级 | 由 rubric 粒度决定，粒度过细会拖慢 |
| 评审人配置 | 高危 2 人独立 + 仲裁；普通 1 人 + 抽检 | LangSmith 的"评审人数/指定评审人"、Braintrust 的"Require all scores"可直接复用 |
| 积压降级 | **fail-closed**：先限流/降级到"建议模式"，绝不因积压而放宽阈值 | 这是最重要的一条：产能不足时宁可少交付 |
| 去重与幂等 | 同 `result_id` 不重复入队；修正后重跑要能自动关闭旧队列项 | 避免队列被重放刷爆 |
| 记录 | 评审人、耗时、是否盲评、是否修正、是否与 judge 不一致 | 既是质量数据也是合规证据 |

### 6.5 从审查到数据：让"转人工"产出资产
人工复核的最大隐性收益是**生产高质量数据**。产品侧已提供标准动作：
- Langfuse corrections：修正稿带 diff，可导出为微调数据集；并明确"修正稿不会自动更新数据集，需人工确认新数据集版本、保留 run/case id 溯源"。
- Braintrust：free-form 分数可写入 `expected` 字段（含嵌套路径），直接把评审结论变成可回归的期望值。
- LangSmith：`Add to Dataset` + assertions 把"验收条件"沉淀为可执行断言。
- Braintrust Topics/Patterns：把日志里的模式转成 scorer、数据集或复核任务。

**闭环要求**：每条被人工判错的绿区样本，必须（a）进入离线回归集，（b）产生一条判据改进项（新增断言/rubric 修改/新探针），（c）在下一版发布前验证。否则同一个错误会反复进队列。

---

## 7. 统计保证：把"我们做到了"变成可辩护的话

### 7.1 指标定义（先说清楚，再谈数字）
| 指标 | 定义 | 用途 |
| --- | --- | --- |
| 残余错误率 R | 自动放行（绿区）结果中最终被判定为错误的比例 | P2 核心指标 |
| 复核覆盖率 | 出厂结果中有"确定性/评分/人工"三类裁决之一的比例 | P1，目标 100% |
| 自动放行率 c | 绿区占比 | 效率 |
| 转交率 ρ | (1−c)+ε·c | 容量规划 |
| 审计发现率 | 随机审计中发现的错误比例（分层统计） | 估计 R、监控漂移 |
| 评审人一致度 | κ / α（含金标准确率） | 人工质量的度量 |
| 判据不确定率 | error/uncertain/skipped 占比 | 产能与判据健康度 |

### 7.2 审计样本量的算术（这是"100%"的报价单）
在二项抽样下，要声称"绿区残余错误率 ≤ α，置信度 95%"：
- 上界用**单侧精确法（Clopper–Pearson）**：满足 P(X ≤ k | p = 上界) = 1 − 置信度。零观测错误时退化为 rule of three：n ≈ 3/α。Wilson 单侧上界可作为快速近似（略偏松，k 小时相差 10% 量级）。
- 若要**检出**（而非仅给上界）一个真实错误率 p，样本量约 n ≈ 3/p 可达到 ~95% 的至少命中一次概率（若用 n ≈ 4/p，命中概率约 98%）。

| 声称的残余错误率上界 α | 零错误所需的审计样本量（95%） | 以 ~95% 概率检出真实错误率 p 所需样本量 |
| --- | --- | --- |
| 5% | ≈ 60 | ≈ 60 |
| 1% | ≈ 300 | ≈ 300 |
| 0.5% | ≈ 600 | ≈ 600 |
| 0.1% | ≈ 3,000 | ≈ 3,000 |

**读法**：如果你想对客户说"我们的自动放行结果错误率低于 0.1%"，你必须每季度抽检约 3,000 件**且全部无误**。这解释了为什么"100% 准确"是预算问题：α 每降一个数量级，审计成本涨十倍。

三个补充：
- **分层审计**：高风险层全量或高抽样率，低风险层低抽样率，可在总样本量不变的情况下更早发现高危问题。
- **顺序审计（SPRT/顺序概率比检验）**：不必等固定样本量，边看边判；适合持续监控，能显著减少平均样本量（Wald 的序贯检验是经典工具）。
- **报告方式**：永远报"点估计 + 置信上界 + 样本量 + 抽样方式"。只报点估计（"错误率 0.3%"）在审计上是无效陈述。

### 7.3 保证会过期：漂移、重校准与自动收紧
- conformal 的有效性依赖校准样本与生产样本**可交换**；域漂移下会欠覆盖〔2510.05566〕。
- 因此必须建立：**定期重校准**（用新人工标签刷新阈值）+ **覆盖率与实测风险监控**（滚动窗口）+ **越界动作**（自动收紧 τ_high，即把绿区样本降级到黄区）。
- 建议做成**自适应控制器**：目标是"实测风险 ≤ α 且人工负载 ≤ 预算"，当两者冲突时，优先满足风险约束（fail-closed）。
- 漂移检测的信号源：新意图聚类（Arize Signal / Braintrust Topics 类）、判据分布漂移、用户负反馈率、转交率异常波动。

### 7.4 保证的作用域声明模板（每次发布/每季度填一次）
> - **覆盖范围**：本保证覆盖 ___（业务场景/意图集合/工具集合/语言/输入长度区间）的 ___% 流量。
> - **判据组成**：L-A 判据 ___ 条（列出）、L-B ___ 条、L-C judge ___ 个（模型与版本、rubric 版本）、人工复核 ___ 类。
> - **未覆盖部分**：___（明确写出），其处理方式为 ___。
> - **度量方式**：随机审计 n=___（抽样方式：分层随机）、Wilson 95% 上界 = ___。
> - **有效期与触发重校准的条件**：___（如判据版本变更、模型版本变更、转交率偏离 ±___%、新意图占比 > ___%）。
> - **失效时的默认动作**：自动收紧阈值至 τ′=___，绿区降级为人工复核（fail-closed）。

---

## 8. 证据链与合规：让"监督"可审计

### 8.1 安全案例结构（claim–argument–evidence）
ML 安全案例工程（AMLAS、SOTIF 思路）已经在汽车/医疗等领域形成成熟范式：把"系统安全"拆成可论证的声明，每个声明由具体证据支撑，并处理"已知未知"与"未知未知"〔相关研究见 2201.05451、2108.13294、2109.00520、2211.04530 与 AMLAS 工业案例〕。映射到本文：

| 声明（Claim） | 论证（Argument） | 证据（Evidence） |
| --- | --- | --- |
| C1 每条出厂结果都有裁决 | 判据流水线对 100% 结果执行；未覆盖者转人工 | 裁决记录表、覆盖率报表（P1=100%） |
| C2 强判据覆盖足够大 | 判据强度阶梯（§1.3）与任务可验证性分级（§3.2） | 各 V 等级的流量占比、L-A 覆盖率趋势（P3） |
| C3 自动放行子集错误率 ≤ α | 阈值由校准集分位数确定；随机审计持续估计 | 审计报表（Wilson 上界）、风险-覆盖曲线 |
| C4 人工复核是可靠的 | 盲评 + 金标 + 一致性 + 仲裁 + 疲劳监控 | κ/α 报表、金标准确率、仲裁记录、单件耗时分布 |
| C5 系统在漂移与攻击下仍保守 | 覆盖率监控、自动收紧、注入检测、监控集成 | 漂移告警与处置记录、攻击测试报告、阈值调整日志 |
| C6 决策可追溯 | 记录 agent/判据/judge/阈值版本与人工结论 | 审计日志、版本绑定记录 |

**注意 C1–C6 的写法**：每条都指向**可复现的证据物**，而不是形容词。这套结构同时也是对监管的答复骨架（§8.3）。

### 8.2 必须落库的审计字段（最小集）
```
result_id, agent_version, task_type, verifiability_level(V1–V4)
judged_at, judge_model, judge_prompt_version, rubric_version, temperature
criteria[]: {criterion_id, type(L-A/L-B/L-C), verdict(pass/fail/uncertain/error), score, evidence_ref}
thresholds_snapshot: {tau_low, tau_high, epsilon, calibration_set_id, calibrated_at}
route: {red|amber|green, triggers[], audit_sampled(bool)}
human_review: {reviewer_id, blind(bool), started_at, duration_s, verdict, corrected_output_ref, confidence}
agreement: {judge_vs_human, inter_reviewer_kappa, gold_item_result}
final_state_evidence: {business_object_id, state_snapshot_ref, verified_by(L-A rule id)}
```
**为什么这些字段**：出问题时能回答三个问题——"当时用哪版判据判的？"、"谁签的字？"、"世界状态真的对了吗？"。Langfuse 的"修正稿不自动进数据集、需人工确认版本、保留原始 run/case id"就是这个思路的产品化。

### 8.3 法规对齐（原文核验，2026-09）
| 来源 | 关键要求 | 对本文体系的要求 |
| --- | --- | --- |
| **EU AI Act 第 14 条（人类监督）** | 高风险系统须设计为"能被自然人有效监督"，含适当的人机界面工具；监督应能预防或最小化对健康、安全、基本权利的风险 | 把"有效监督"落成具体机制：盲评、金标、仲裁、容量与疲劳管理（自动化偏见正是"监督无效"的典型失效）；条文生效时间按 113(c)：**2027-12-02**（附件 III 高风险）/ **2028-08-02**（附件 I） |
| **EU AI Act 第 15 条** | 准确性、鲁棒性与网络安全相关要求 | 对应残余错误率上界 + 漂移/攻击鲁棒性证据（§7.3、§4.6） |
| **NIST AI RMF** | 风险管理框架（治理/映射/度量/管理），强调持续测量与文档化 | 对应 §7 的度量与 §8.1 的证据链 |
| **中国《生成式人工智能服务管理暂行办法》（2023-08-15 施行）** | 第四条(五)：提升透明度、**提高生成内容的准确性和可靠性**；**第八条**：数据标注须有清晰具体可操作的**标注规则**、开展**标注质量评估**、**抽样核验标注内容的准确性**、对标注人员培训；第十七条：具舆论属性/社会动员能力者须**安全评估 + 算法备案**；第十九条：监督检查时须能说明**标注规则**等 | **第八条几乎逐字对应本文体系**：标注规则 ≈ rubric/判据规范；标注质量评估 ≈ judge 与人工的一致性验收；**抽样核验** ≈ ε 随机审计（这正是"抽样"两个字的监管来源） |
| **中国《人工智能生成合成内容标识办法》（2025-09-01 施行）** | 显式标识（图片/视频/音频/虚拟场景的具体位置要求）+ 文件元数据中的**隐式标识**（生成合成属性、服务提供者名称/编码、内容编号）；下载/复制/导出须保留显式标识；服务协议中说明标识方法；无显式标识的提供需留存相关日志**不少于六个月**；算法备案与安全评估时提交标识材料 | 结果交付层必须带标识与元数据；审计日志留存 ≥ 6 个月——**直接影响审计样本的存储设计** |
| ISO/IEC 42001（AI 管理体系） | 管理体系层面的要求 | 本文未取证原文（标准付费），仅作为体系框架参考 |

### 8.4 三种说法（同一套机制，三种受众）
- **对领导**："我们承诺的不是零错误，而是零未复核。绿区残余错误率每季度用 3,000 件随机抽检复验，95% 置信上界 ≤ 0.1%；审计成本 X 人日/季度。"
- **对客户**："自动放行结果带质量声明与作用域；超出作用域的请求自动转人工，不会静默放行。"
- **对监管**："标注规则、质量评估、抽样核验、人员培训、日志留存、算法备案，逐条对应到我们的 ___ 文件与 ___ 报表。"

---
## 9. 产品与开源选型（star / 版本为 2026-09-22 快照）
### 9.1 在线评测与可观测平台
| 类型 | 代表 | 实测要点 | 适用 |
| --- | --- | --- | --- |
| 闭源 SaaS（评测最完整） | LangSmith、Braintrust、Arize AX、Galileo(→Splunk)、Humanloop、Patronus | 在线 evaluator + **内置人工队列** + 数据集回流一体；采样率、花费上限、盲评、条件显示等运营细节最全 | 想要"一周内跑起来"，接受数据出域 |
| 开源自建 | Langfuse（34.9k★, v4.41.0）、Phoenix（11.6k★）、Opik（22.2k★, 2.2.75）、Evidently（7.9k★, v0.7.23）、OpenLLMetry（7.4k★） | Langfuse 的 queue + corrections + judge 三件套最贴近本文；Phoenix 的 tool-calling 结构化判分最规范；Opik 队列锁 API 适合自建 | 私有化/数据不出域（国内常见） |
| 测试与断言 | promptfoo（25.4k★, 0.123.1）、inspect_ai（2.8k★）、DeepEval（18.4k★, py v4.2.0）、Ragas（15.8k★, v0.4.3，仓库已迁至 vibrantlabsai/ragas） | 适合 CI 门禁与回归；在线路由能力弱 | 工程团队自建流水线 |
| 托管云 | AWS AgentCore Evaluations、Azure AI Foundry（连续评测 + agent evaluators）、Vertex Gen AI evaluation service | 与自家 trace/告警天然打通；采样率与阈值配置化；缺点是判据可定制性有限、跨云不互通 | 已深度绑定某云 |

### 9.2 人工复核与标注底座
| 需求 | 首选 | 备选 |
| --- | --- | --- |
| 与评测平台一体 | LangSmith / Braintrust / Langfuse 自带队列 | Opik、Giskard Hub |
| 私有化、通用标注 | Label Studio（28.3k★, 1.23.0） | Argilla、doccano（10.8k★） |
| 标注质量与一致性 | cleanlab（11.7k★, v2.9.0） | 自建 κ/α 统计 |
| 红队 + 失败分类 + 调度 | Giskard Hub（OSS 5.8k★） | Azure 定时红队 |
| 通用人工审核工作流服务 | AWS A2I（文档未取到正文，需另核） | 自建队列 |

### 9.3 不确定性与风险评估件（给判据加"自知之明"）
| 用途 | 项目（2026-09） |
| --- | --- |
| LLM 不确定性量化工具箱 | uqlm（1.2k★, v0.6.6，含多类黑盒 UQ 方法） |
| 接地/事实校验小模型 | MiniCheck（227★）、SelfCheckGPT（631★）、Vectara hallucination-leaderboard（3.3k★） |
| 规则与护栏 | guardrails-ai（7.4k★, v0.11.0）、NeMo Guardrails（7.2k★, v0.24.1） |
| Agent 级评测/基准 | tau2-bench（2.1k★, v1.0.1）、agent-as-a-judge（828★） |

### 9.4 选型决策树（一句话版）
1. 数据可出域 → 直接用 LangSmith/Braintrust/Arize，先跑通再自研。
2. 必须私有化 → Langfuse（云端/自托管）+ Label Studio（大规模人工）+ Giskard（红队），judge 用自选模型。
3. 已在某云上 → 先用该云连续评测把 P1/P2 指标建起来，判据定制部分用开源补齐。
4. 有强工程团队且要深度定制 → Phoenix/Opik + 自建路由与校准层（本文 §5 的阈值与校准逻辑自研成本可控）。

---

## 10. 成本模型与容量规划（工程估算，非实测）
### 10.1 四个成本项
| 成本项 | 公式 | 备注 |
| --- | --- | --- |
| 判据成本 | 流量 × 判据命中率 × 单次判据成本 | 串联可降：L-A 不过则跳过 judge；同组信号批量调（Weave 做法） |
| 人工成本 | 流量 × 转交率ρ × 单件耗时 × 人力单价 | ρ = (1−c) + ε·c |
| 审计成本 | 每季度审计样本量 n × 单件复核耗时 × 人力单价 | 由 α 决定，§7.2 |
| 错误成本 | 误放行率 × 单次损失（期望损失） | 决定 α 该定多严；用"钱"的语言比"准确率"更容易达成共识 |

### 10.2 用期望损失定阈值（比拍脑袋更可辩护）
对给定阈值 τ，令：误放行概率 p_fp(τ)、漏拦概率 p_fn(τ)、单次误放行损失 C_miss、单次人工成本 C_rev。总成本：
`TotalCost(τ) = 流量 × [ p_fp(τ)·C_miss + 转交率(τ)·C_rev + 判据成本 ]`
在人工容量约束下最小化 TotalCost → **这就是"三区间"里 τ_low/τ_high 的定量依据**。相关的工作里，"期望损失"已被做成 agent 评测/控制的显式目标（如 trajectory-level、按严重度加权的损失基准思路），比"平均分"更贴近业务。

### 10.3 容量红线（必须先算）
- 转交率 ρ 一旦超过 `可用人工工时 / 单件耗时`，队列必然积压 → 触发 fail-closed 降级（限流/建议模式）。
- 因此**上线前必须做容量反算**：给定 α 与审计预算，ρ 的上限是多少，超出后业务侧接受"少交付"而不是"放宽阈值"。

---

## 11. 落地路线图
### 阶段 1（0–30 天）：看得见 + 拦得住
1. 定义"结果"：为每个 agent 写出**终态定义**（世界状态）与可验证性等级 V1–V4。
2. 把 L-A 判据先堆上去：断言、schema、状态回读、幂等重放。目标：L-A 覆盖率有基线数字。
3. 建立人工队列（Langfuse/Label Studio 二选一），先覆盖"高危动作 + 硬触发清单"。
4. 建三条旁路的骨架：随机审计抽样、金标夹带、用户负反馈入口。
5. 输出第一版 §8.2 审计字段与作用域声明模板（先填 TBD 也行）。

### 阶段 2（30–90 天）：评分、校准、路由闭环
6. 上 judge（pointwise + 结构化输出 + 举证），先在离线黄金集上与人工对齐（κ 报表），再上线。
7. 建校准集，按 conformal 分位数定 τ_low/τ_high；建立风险-覆盖曲线。
8. 上三区间 + 双保险路由；把 judge 的四项 SLO（§4.4）接入监控与告警。
9. 静默故障专项：加步骤级 rubric + 故障注入探针（照搬 trajectory-judge 的实验设计思路）。
10. 修正稿 → 数据集 → 回归的自动链路（含版本与溯源）。

### 阶段 3（90–180 天）：保证、漂移、证据链
11. 残余错误率进入季度报表（Wilson 上界 + 样本量 + 抽样方式），并写进对外承诺。
12. 自适应控制器：实测风险 vs 人工负载双约束，越界自动收紧（fail-closed）。
13. 人工侧治理：金标准确率、κ/α、仲裁、疲劳与负荷监控（队列长度作为状态变量）。
14. 安全案例文档化（§8.1 六条声明 + 证据物清单），对齐 EU AI Act 14/15 与国内第八条的检查项。
15. 演练：阈值收紧、队列熔断、回滚判据版本（"刹车能力必须演练"）。

---

## 12. 反模式清单（审查在线评价方案时逐条对照）
1. **把"100% 准确"直接写进 SLO**，用"零错误"替代"零未复核"。
2. **只审低分样本**：绿区无随机审计 → 残余错误率在数学上不可估计。
3. **阈值拍脑袋**（"0.8 感觉比较安全"），没有校准集、没有分位数依据。
4. **judge 升级不做重基线**：判据版本变了，阈值、数据集、历史对比全部失效。
5. **用 exact match 衡量 judge 与人工的一致性**（kappa 虚高 33–41pp）。
6. **用同一个模型既生成又评判**，且不做跨家族集成。
7. **把 judge 的每一条输出当有效**：不区分 pass/fail/uncertain/error，`error` 默认放行。
8. **加权平均吞掉高危项**：安全判据不是 veto，而是"扣几分"。
9. **只评最终文本**，不评轨迹与终态 → 静默故障漏检率过半。
10. **评审人能看到 judge 的分数与理由**（锚定 + 自动化偏见），且不做盲评。
11. **把人工标签直接当金标**：不测评审人准确率、不测 κ、不做仲裁。
12. **把 UI 的"分数可见性"当权限边界**（Braintrust 文档已明确说明其可被 `Show all scores` 绕过）。
13. **队列 FIFO 且无 SLA**；积压时默默放宽阈值而不是限流降级。
14. **不监控转交样本的业务分布**：对少数类的系统性过渡转交会污染数据集。
15. **无金标夹带**：无法区分"判据变好了"和"评审人变松了"。
16. **审计报表只给点估计**，不给置信区间、样本量与抽样方式。
17. **把保证写成永久有效**：不声明作用域、不做重校准、不设失效动作。
18. **不把线上错误回灌离线回归集**：同一个错误反复进人工队列。
19. **人工队列不产生资产**：修正稿不落库、不版本化、不进数据集。
20. **判据与输入不隔离**：被评内容可以直接向 judge 下指令（注入面）。

---

## 13. 参考与取证材料
### 13.1 论文（arXiv 实时检索，2026-09-22）
**综述（用于定位领域切分）**
- 2507.21504 Evaluation and Benchmarking of LLM Agents: A Survey（评什么 × 怎么评 二维分类；企业场景挑战与可靠性保证）
- 2411.15594 A Survey on LLM-as-a-Judge（judge 的方法、可靠性与标准化）
- 2606.04990 From Agent Traces to Trust: A Survey of Evidence Tracing and Execution Provenance in LLM Agents
- 2603.04445 Dynamic Model Routing and Cascading for Efficient LLM Inference: A Survey

**选择性评估 / 保证 / 转交**
- 2407.18370 Trust or Escalate: LLM Judges with Provable Guarantees for Human Agreement（选择性评估 + 人类一致性保证 + Simulated Annotators + 级联选择性评估）
- 2208.02814 Conformal Risk Control（控制任意单调损失的期望；FNR、token-F1 等）
- 2512.12844 Selective Conformal Risk Control（选择 + 风险控制两阶段，有限样本保证）
- 2403.01216 API Is Enough: Conformal Prediction for LLMs Without Logit-Access
- 2510.05566 Domain-Shift-Aware Conformal Prediction for LLMs（漂移导致欠覆盖）
- 2604.13991 Adaptive Conformal Prediction for Improving Factuality（prompt 自适应 + 选择性预测）
- 2603.15574 Severe Domain Shift…Uncertainty Failure（高 OOD AUROC 不保证安全选择性分类；50% 覆盖下风险 99.6%）
- 2405.02917 Overconfidence is Key: Verbalized Uncertainty Evaluation（语言模型口头不确定性的过度自信）
- 2304.07306 Learning to Defer with Limited Expert Predictions
- 2509.12573 No Need for Learning to Defer?（免训练、conformal 驱动、多专家）
- 2403.06906 Cost-Sensitive Learning to Defer to Multiple Experts with Workload Constraints（DeCCaF）
- 2604.00904 Fatigue-Aware Learning to Defer via Constrained Optimisation（FALCON + FA-L2D）
- 2608.28050 Too Much of the Same: From Algorithmic to Human Bias in Learning to Defer（转向少数类的采样偏置；N=226 用户研究）
- 2312.13218 FiFAR: A Fraud Detection Dataset for Learning to Defer（50 名合成分析师）

**judge 可靠性 / rubric 工程**
- 2606.19544 Reliability without Validity（21 judge / 9 供应商 / ~541,000 判定；κ 与 exact match 差 33–41pp；排名移动 14 位；位置偏置 >0.10）
- 2606.13685 The Coin Flip Judge?（翻转率 13.6%；首位偏置 72%；pairwise–pointwise gap）
- 2506.22316 Evaluating Scoring Bias in LLM-as-a-Judge（rubric 顺序偏置、score ID 偏置、参考答案分数偏置）
- 2602.09383 BiasScope（自动发现未知偏置）
- 2605.06939 Bias and Uncertainty in LLM-as-a-Judge Estimation（估计量偏差；共享校准可致符号反转）
- 2601.08654 From Rubrics to Reliable Scores: Evidence-Grounded Text Evaluation with LLM Judges（Rulers）
- 2602.05125 Rethinking Rubric Generation…（RRD：分解-过滤-去相关加权）
- 2605.30803 PReMISE: Policy Rubrics as Measurement Specifications
- 2310.02743 Reward Model Ensembles Help Mitigate Overoptimization
- 2505.12763 Rethinking Reward Model Evaluation Through the Lens of Reward Overoptimization

**Agent 结果与轨迹评测**
- 2609.00038 trajectory-judge: What Outcome-Only LLM Judges Miss on Agent Trajectories（响亮 84% / 静默 45% / 误报 33%）
- 2510.04550 TRAJECT-Bench（工具选择、参数、依赖顺序的轨迹级诊断）
- 2608.30685 ATLAS: Dual-Horizon Diagnostic Evaluation for Industrial Tool-Use Agents
- 2608.20614 Evaluating Skills, Not Just Agents: Agentic Continuous Evaluation of Skills

**监控/控制/安全案例**
- 2510.09462 Adaptive Attacks on Trusted Monitors Subvert AI Control Protocols（注入绕过 monitor）
- 2608.16190 Decorrelation Is Not Complementarity（24 monitor / 9 谱系；集成增益由技能决定）
- 2602.09902 Routing, Cascades, and User Choice for LLMs；2605.18859 TwinRouterBench
- 2411.00998 Automation Bias in AI-Assisted Medical Decision-Making under Time Pressure（7% 自动化偏见；n=28）
- 2306.16507 Bending the Automation Bias Curve
- 2201.05451 A causal model of safety assurance for ML；2108.13294 感知组件安全案例；2109.00520 医疗 XAI 保证；2211.04530 野火告警 ML 安全案例（含 AMLAS 实践）

### 13.2 产品与厂商官方文档（2026-09 实测，`research/vendor3/`）
- LangSmith：`ls-annotation-queues.txt`、`ls-eval-concepts.txt`、`ls-llm-as-judge.txt`、`ls-multiturn-online.txt`（含 sampling rate、spend limit、reservations、盲评、Add to Dataset）
- Langfuse：`lf-annotation-queues.txt`、`lf-llm-as-a-judge.txt`、`lf-online-getstarted.txt`、`lf-corrections.txt`、`lf-user-feedback.txt`
- Braintrust：`bt-human-review.txt`（blind reviews、条件显示、Require all scores）、`bt-score-online.txt`
- Phoenix / Arize：`phoenix-evals.txt`、`arize-ax-root.txt`
- Opik / W&B Weave / Galileo / Giskard：`opik-concepts.txt`、`opik-overview.txt`、`weave-monitors.txt`(vendor2)、`galileo-docs.txt`(vendor2)、`giskard-docs.txt`、`giskard-eval-review.txt`
- 云厂商：`aws-agentcore-evals.txt`(vendor2)、`aws-autoreasoning.txt`(vendor2)、`azure-agent-evaluators.txt`、`azure-foundry-obs.txt`、`azure-continuous-eval.txt`(vendor2)、`vertex-eval.txt`
- 标注平台：`argilla-gh-readme.txt`、`labelstudio-guide2.txt`、`evidently-gh.txt`、`cleanlab-docs.txt`
- OpenAI：`oai-evals.txt`、`oai-graders.txt`（注意：官方已公告在 evals/fine-tuning 工作流中**弃用 graders**，以官方 deprecations 页为准）
- 法规原文：`eu-ai-act-art14.txt`、`eu-ai-act-art15.txt`、`nist-ai-rmf.txt`、`cac-genai-measures2.txt`、`cac-labeling2.txt`

### 13.3 项目元数据
- `research/meta6.txt`：本轮新增 50 个相关项目的 star / 最新 release / 最近提交 / 许可证（2026-09-22 快照）
- `research/gh_scan_online_eval.txt`：14 组主题检索结果
- `research/fetch_online_eval*.ps1`：本轮五轮抓取的复现脚本（第 5 轮为综述定位检索，结果见 `research/fetch_online_eval5.log`）

### 13.4 取证说明（诚实标注）
- **AWS 文档站为 JS 渲染**：AgentCore Evaluations 的 `online-evaluation` / `built-in-evaluators` 子页与 SageMaker A2I 页面只返回了脚本壳（21–26 字符），本文对 AWS 的描述仅基于可取得的落地页与本仓库既有取证文件；A2I 的能力描述**未经原文核验**。
- 少数页面因网络中断未取到：`opik-annotation-queues`（已由 Opik 文档索引确认其存在与用途）、`evidently-monitor`、`lf-sampling`、`lf-scores`。
- ISO/IEC 42001 未取得原文，仅作体系框架提及。
- §10 的成本数字、§7.2 之外的样本量推导为**工程估算**，不是实测。

---

## 附录 A：给领导的 1 页速查
1. **目标**：不是"零错误"，是"**零未复核出厂**"——每条结果都有裁决记录。
2. **三个数字**：复核覆盖率 100%；绿区残余错误率 ≤ α（95% 上界）；强判据覆盖率 ≥ v。
3. **为什么不能让 judge 说了算**：judge 同题翻转 13.6%、kappa 虚高 33–41pp、只看结果会漏掉 55% 的静默故障，还能被 prompt injection 绕过。
4. **机制**：红区不放行 / 黄区必审 / 绿区自动放行 + 随机审计；盲评 + 金标 + 仲裁防橡皮图章。
5. **代价**：声称 ≤0.1% 需要每季度约 3,000 件零错误抽检；α 降一个数量级，审计成本涨十倍。
6. **监管对齐**：国内《生成式 AI 服务管理暂行办法》第八条要求的"标注规则 + 质量评估 + 抽样核验 + 人员培训"与本文机制逐条对应；EU AI Act 第 14 条"有效监督"对应盲评与容量管理。

## 附录 B：阈值与样本量速算（交互版：[`online-eval-calculator.html`](online-eval-calculator.html)）
| 想回答的问题 | 公式 | 示例 |
| --- | --- | --- |
| 绿区错误率上界（零观测错误） | α_ub ≈ 3/n | n=300 → ≤1% |
| 绿区错误率上界（观测 k 次错误于 n 样本） | 单侧精确上界（Clopper–Pearson，95%） | n=300：k=0 → 0.99%；k=3 → 2.56%；k=10 → 5.59% |
| 以 95% 概率检出真实错误率 p | n ≈ 3/p | p=1% → n≈300 |
| 人工负载 | 流量 × [(1−c) + ε·c] × 单件耗时 | 2 万 × 10% × 3min = 6,000 分钟/日 |
| 阈值定价 | 总成本 = 流量 × [p_fp(τ)·C_miss + ρ(τ)·C_rev] | 由期望损失最小化定 τ |
| 顺序审计 | 用 SPRT 边看边判，平均样本量低于固定样本 | 持续监控场景 |
